\subsection{Hiện thực Database}
Dưới đây là các câu lệnh sql để tạo một số bảng chính trong hệ thống:
\begin{lstlisting}[language=sql]
    CREATE TABLE users (
        id BIGSERIAL PRIMARY KEY,
        role role_type,
        username VARCHAR(50) NOT NULL UNIQUE,
        password VARCHAR(255),
        email VARCHAR(100) UNIQUE,
        full_name VARCHAR(100),
        gender gender_type,
        dob DATE,
        status user_status NOT NULL DEFAULT 'ACTIVE',
        avatar TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ
    );
    
    CREATE TABLE place (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        name VARCHAR(255) NOT NULL UNIQUE,
        city_code VARCHAR(50),
        codename VARCHAR(50),
        country VARCHAR(100),
        country_code VARCHAR(10),
        region VARCHAR(100),
        latitude DECIMAL(10, 8),
        longitude DECIMAL(11, 8),
        google_place_id VARCHAR(255)
    );
    
    CREATE TABLE place_of_interest (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        name VARCHAR(255) NOT NULL,
        poi_type poi_type_enum,
        poi_type_detail TEXT,
        system_status poi_status_enum   DEFAULT 'ACTIVE' NOT NULL,
        address TEXT,
        website TEXT,
        phone_number VARCHAR(20),
        latitude  DECIMAL(10, 8),
        longitude DECIMAL(11, 8),
        rating DECIMAL(3, 2),
        review_count INT,
        status place_status_enum NOT NULL DEFAULT 'OPERATIONAL',
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP,
        google_maps_url TEXT,
        description TEXT,
        google_place_id VARCHAR(255) UNIQUE,
        opening_hour JSONB,
        reviews_distribution JSONB,
        additional_info JSONB,
        place_id BIGINT REFERENCES place(id) ON DELETE SET NULL
    );
    
    CREATE TABLE review (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        google_review_id VARCHAR(255) UNIQUE,
        status review_status_enum not null default 'ACTIVE',
        content TEXT,
        rating DECIMAL(3, 2),
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP,
        external_name VARCHAR(255),
        external_avt TEXT,
        is_crawled BOOLEAN NOT NULL DEFAULT FALSE,
        place_of_interest_id BIGINT REFERENCES place_of_interest(id) ON DELETE SET NULL,
        traveler_id BIGINT REFERENCES traveler(id) ON DELETE SET NULL
    );
\end{lstlisting}
Hình dưới đây thể hiện lược đồ quan hệ của một số bảng chính của hệ thống cơ sở dữ
liệu, được xuất từ công cụ DBeaver sau khi định nghĩa và thiết lập các bảng, khóa chính, khóa ngoại và các liên kết liên quan.
\begin{figure}[htp]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/8. Implement/database/ldqh.png}
    \caption{Lược đồ quan hệ đầy đủ của hệ thống cơ sở dữ liệu của TravelEZ}
    \label{fig:db-erd}
\end{figure}